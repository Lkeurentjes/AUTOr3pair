# content of pytest.ini
[pytest]
markers =
    build: check if builds are all right
    full: run full set of tests
    keep: keep all repairs instead of deleting
    NotFinished: Tests of not implemented repairs

testpaths = tests